const initState = {
    position: [],
    polygonShown:[],
    polygon:[],
    polyline:[],
    markers:[],
}

const mapReducer = (state = initState, action) => {
    return state;
}

export default mapReducer
